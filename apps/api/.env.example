# API port
PORT=3001

# Frontend origin for dev CORS + cookies
WEB_ORIGIN="http://localhost:3000"

# JWT secret for signing access tokens
JWT_SECRET="dev-only-secret-change-me"

# Local PostgreSQL for the learning project
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pulse_chat?schema=public"
